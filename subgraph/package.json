{
  "name": "spectre",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ spectre",
    "create-local": "graph create --node http://localhost:8020/ spectre",
    "remove-local": "graph remove --node http://localhost:8020/ spectre",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 spectre",
    "test": "graph test -v 0.5.2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.47.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
